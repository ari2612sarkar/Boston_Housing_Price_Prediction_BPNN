# Project 4 — Backpropagation Neural Network from Scratch (NumPy Only)

## 📌 Overview
This project implements a **Backpropagation Neural Network** from scratch using only NumPy — with one hidden layer.  
The model is trained on the **Boston Housing dataset** and evaluated using **5-fold and 10-fold cross-validation**.

---

## 🧱 Network Architecture
- **Input Layer:** User-defined size (based on dataset)
- **Hidden Layer:** Configurable (3, 4, or 5 neurons)
- **Output Layer:** 1 neuron (for regression)
- **Activation:** Sigmoid
- **Loss Function:** Mean Squared Error (MSE)
- **Optimizer:** Basic Gradient Descent

---

## 🚀 How to Run

### 1️⃣ Install Requirements
Only NumPy, Pandas, and Matplotlib are required.
```bash
pip install numpy pandas matplotlib
